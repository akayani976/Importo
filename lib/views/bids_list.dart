import 'package:flutter/material.dart';

class BidsListPage extends StatelessWidget {
  const BidsListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Your bid has been registered'),
      ),
    );
  }
}
